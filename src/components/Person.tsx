import * as React from 'react';

export interface Props {
    name: string;
    age: number;
    email: string;
}

export const Person  = (props: Props) => {
  return (
    <div>
      
    </div>
  );
}
